{
  "name": "iclaw-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:bindings": "bash ../scripts/generate_ic_ui_bindings.sh",
    "dev": "npm run generate:bindings && vite",
    "build": "npm run generate:bindings && tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm run generate:bindings && vitest run",
    "test:e2e": "npm run generate:bindings && playwright test",
    "test:e2e:headed": "npm run generate:bindings && playwright test --headed"
  },
  "dependencies": {
    "@dfinity/agent": "^2.4.1",
    "@dfinity/auth-client": "^2.4.1",
    "@dfinity/candid": "^2.4.1",
    "@dfinity/principal": "^2.4.1",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.1"
  },
  "devDependencies": {
    "@dfinity/internet-identity-playwright": "^2.0.0",
    "@playwright/test": "^1.58.2",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^25.3.0",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^26.1.0",
    "tailwindcss": "^4.0.0",
    "typescript": "~5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.2.4"
  }
}
